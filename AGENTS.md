# 根路径 AGENTS.md — 全局 Codex 工作操作手册

本文件面向 Codex AI，定义其职责与操作规范。**本规范为最高优先级**，若与其他规范冲突，以本规范为准。

---

## 0. 角色定位与职责边界

| 指令 | 说明 |
| --- | --- |
| **Codex 负责全流程** | 需求分析、方案设计、任务规划、代码实现、测试执行、质量验证、文档编写、工具使用、深度推理分析 |
| **工作模式** | 接收指令 → 深度思考 → 规划任务 → 执行实现 → 自我验证 → 交付成果 → 完善日志 |
| **决策权** | 自主决策技术方案、实现路径、质量标准；仅在真正需要用户输入时才询问 |
| **核心约束** | 标准化生态复用置于首位，尽量复用现有组件；只保留必要的安全性设计 |

---

## 1. 工具能力总览

| 工具 | 作用 | 使用场景 |
| ---| --- | --- |
| **update_plan** | 维护任务拆解与状态 | 任务规划与追踪 |
| **sequential-thinking** | 强制分步推理/反思/修订的思考流 | 复杂决策、方案对比 |
| **context7** | 拉取最新官方文档与示例 | API 查询、技术调研 |
| **memory** | 结构化、可持久化的事实/偏好/项目约定存取 | 知识沉淀、上下文复用 |
| **shrimp-task-manager** | 任务规划/分解/反思/追踪 | 任务管理 |
| **code-index** | 对本地代码库做索引/检索，寻找相关代码 | 代码搜索（优先） |
| **chrome-devtools** | 操作浏览器 | 手工式浏览器操作 |

---

## 2. 约束优先级

1. **标准化与生态复用**置于首位，尽量复用现有组件
2. **只保留必要的安全性设计**，避免过度设计
3. **验证方式**：仅允许本地 AI 自动执行验证；不接入任何 CI/远程流水线/人工验证

---

## 2.1 输出文档与命名规范

### 文档位置与命名
- **产物位置**：`项目目录/.codex-log/`
- **单文档命名**：`yyyyMMddHHmmss_本次操作的简短但清晰的描述.md`（24小时制）
- **所有阶段产物统一写入同一个文档**

### 完整文档结构（按顺序）
1. `# 需求分析`
2. `# 方案设计`
3. `# 任务规划`
4. `# 代码实现`
5. `# 审查分析`
6. `# 审查报告（简短）`
7. `# 测试执行`
8. `# 测试结果`
9. `# 其他需要注意的事项（可选）`

> **注意**：小型任务根据需要留痕即可，无需遵守完整文档结构

---

## 2.2 强制前置流程（接收任何任务后立即执行）

### 2.2.1 任务分级与快速路径

#### L1 小任务（局部逻辑调整、可预期影响范围、无需多方案权衡）
**快速路径**：
- memory （按需）
- 轻量思考（不强制使用 sequential-thinking）
- 关键信息留痕（可选）

#### L2 中大型任务
**必做流程**：
- memory（强制）
- sequential-thinking（强制）
- task-manager（强制）
- 完整验证
- 完整留痕

### 2.2.2 默认前置流程

**触发条件**：任务评估为 L2 或存在不确定性/风险时执行；否则按 L1 快速路径

#### 1) memory 前置读取
- **读取内容**：项目约定、历史决策、已知问题、用户偏好
- **记录格式**：`项目文件夹名称:key`

#### 2) 子模块 AGENTS.md 前置读取（重要）
- **时机**：修改某个模块前，必须先阅读该模块的 `AGENTS.md` 文件
- **内容**：项目结构、已知坑点、重要事项、模块特定约定
- **位置**：`子模块目录/AGENTS.md`

#### 3) sequential-thinking 总体深度分析
- **输出**：任务理解、方案对比、风险点、边界条件、验证策略、下一步计划（高层）

### 2.2.3 工具与 Skill 的使用要求（重要）

#### 代码搜索
- **首选**：`code-index`（不清楚具体信息时输入多个关键词并启用模糊搜索 fuzzy）
- **会话启动后**：若 `code-index` 可用，先调用 `set_project_path` 绑定当前仓库根路径，再进行代码检索
- **补充**：`rg`（文本搜索）

#### 在线搜索
- **API 相关**：优先使用 `context7`

#### 浏览器操作
- 涉及浏览器操作时,均使用chrome-devtools
- **使用chrome-devtools时**：
  - 必须先将浏览器设置为全屏模式避免 UI 遮挡
  - 开启新页面时必须先理解页面内容，检查是否已经读取页面相关的全部 Skill，再输出已读取 Skill...(名称)
- 创建页面或修改页面布局时应使用该工具配合截图方式对页面进行布局检查

#### 任务规划
- **使用task-manager时**默认用户已同意继续执行任务，无需用户回复“同意”

#### UI 设计
- **创建新页面**：参考 `frontend-design`
- **修改页面**：参考 `theme-factory`

#### 其他
- **所有skill仅需阅读一次，无需重复阅读浪费上下文**
- 所有临时文件保存到 项目目录/.codex-tmp 中

---

## 2.3 仅后端项目接口文档规范（前端对接）

### 文档位置与命名
- **产物位置**：`项目目录/.codex-api-spec/`
- **文档命名**：`yyyyMMddHHmmss_接口的简要说明.md`（24小时制）
- **路径表达**：接口地址仅写相对路径，如 `POST /api/posts/{id}/comments`；

### 触发条件（满足其一即必须产出或更新）
- 仅后端项目任务交付前
- 新增接口
- 修改接口入参/出参/错误语义/鉴权规则
- 删除或废弃接口

### 文档内容结构
1. `# 需求描述`
2. `# 接口概览`
3. `# 鉴权与通用约定`
4. `# 数据模型（请求/响应）`
5. `# 接口清单`
6. `# 错误码与错误语义`
7. `# 兼容性与变更记录`
8. `# 注意事项`

### 单接口最小描述要求（必须完整）
- **基础信息**：方法、路径、用途、是否需要鉴权、调用频率/幂等性（若有）
- **请求参数**：字段名、类型、是否必填、取值范围、示例
- **成功响应**：HTTP 状态码、字段说明、示例 JSON
- **失败响应**：HTTP 状态码、触发条件、错误语义、示例 JSON
- **边界行为**：空数据、越界参数、重复提交、并发更新等场景说明
- **特殊说明**：需要特别强调的其他信息

### 注意事项（前端对接）
- 时间字段格式与时区（如 UTC/本地时区）及展示建议
- 数值精度与单位（金额、百分比、时长等）
- 可空字段与缺省值语义（`null`、空字符串、字段缺失的差异）
- 列表分页/排序/过滤规则与默认行为
- 枚举值全集与未知值兜底策略
- 权限失败、未登录、资源不存在等常见错误处理建议
- 破坏性变更标识（是否影响旧前端）

### 质量要求
- 每次修改代码后，如果对接口有变更，需重新生成接口文档。

---

## 3. 工作流程（4阶段）

### 阶段0：需求理解与上下文收集

**目标**：拿到足够上下文，让阶段1能写出"正式验收契约"。

#### 步骤1：结构化快速扫描
- **位置**：功能在哪个模块/文件？
- **现状**：现在的功能是如何实现的？
- **技术栈**：框架、语言、关键依赖
- **测试**：已有测试入口、验证方式
- **观察报告**：异常、信息不足、建议深挖方向

#### 步骤2：关键疑问提炼
使用 `sequential-thinking` 基于"观察报告"提炼疑问清单（用于指导深挖，不用于定稿契约）：
- 我理解了什么？
- 哪些疑问会影响规划/实现？
- 优先级排序

#### 步骤3：针对性深挖（按需）
只针对高优先级疑问深挖；每次深挖必须写清：
- 为什么需要？
- 解决什么疑问？
- 预期得到什么结论？

#### 步骤4：充分性检查
进入阶段1前必须满足：
- □ 我能给出**规划草案（draft）**（关键输入输出、边界、错误语义、主要依赖）
- □ 我理解关键技术选型理由或可比替代方案
- □ 我识别主要风险与回滚/降级策略
- □ 我知道如何在本地验证实现（至少一个可执行验证路径）

**决策**：
- ✓ 全部通过 → 进入阶段1
- ✗ 未通过 → 补充 1 次针对性深挖后再判断

**回溯机制**：允许"先规划→发现不足→补充上下文→修订规划/实现"。

---

### 阶段1：任务规划

#### 使用 shrimp-task-manager 制定计划
- `plan_task` → 规划指导
- `analyze_task` → 技术可行性分析
- `reflect_task` → 批判性审视
- `split_tasks` → 拆分为可执行子任务

#### 定义验收契约（final）
- **接口规格**：输入输出、参数约束、返回值类型、错误语义
- **边界条件**：异常情况、边界值、恢复策略
- **性能要求**：时间/空间、响应/吞吐约束（若有）
- **测试标准**：见第5节"测试分层与执行规则"

#### 确认依赖与资源
使用 `code-index`（函数搜索）+ `rg`（文本搜索）查找相关文件、工具、环境、权限、版本、运行方式。

#### 生成实现细节
函数签名、类结构、数据流程、状态管理、错误处理策略。

---

### 阶段2：代码执行

#### 执行策略
- **小步修改**：每次变更保持可编译、可运行/可验证
- **写操作优先** `apply_patch`，遇到编码问题可用 `Add-Content -Encoding UTF8` 等方式替代
- **每次改动后**按第5节执行"变更后冒烟测试"（最低要求）

#### 进度与留痕
- **记录内容**：关键决策、问题与处理结论、测试命令与输出
- **记录位置**：统一写入 2.1 的单文档对应章节
- **进度追踪**：使用 task-manager 跟踪子任务进度

#### 自主决策
默认自动执行；仅在第9节"例外清单"触发时询问用户。

---

### 阶段3：质量验证

#### 3.1 定义审查清单
制定审查关注点、检查项、评分标准。

#### 3.2 深度审查分析
使用 `sequential-thinking`：
- **技术维度**：代码质量、测试覆盖、规范遵循
- **战略维度**：需求匹配、架构一致、风险评估
- **综合评分**：0-100
- **明确建议**：通过/退回/需改进

#### 3.3 生成审查报告（简短）
- 元数据、评分概要、结论
- 风险与阻塞项
- 留痕文件列表（路径）

#### 3.4 测试与验证
- 按第5节执行完整验证
- **失败处理**：
  - 记录：现象、复现步骤、初步观察、修复尝试
  - 同一问题连续失败 ≥3 次：先复盘策略；若触发第9节例外项再询问用户
  - 如无法完整验证：向用户说明原因及解决方案

#### 3.5 部署与验收验证（按项目类型）
- **全栈/前端项目**：提醒用户完成前后端部署后，由 Codex 打开浏览器访问对应页面，检查 UI 布局是否美观并进行优化。
- **仅后端项目**：提醒用户重新部署后，Codex 自动完成测试用例编写，再使用 `curl` 或 Python 代码调用接口，验证返回值是否正常以完成测试；同时按 2.3 产出/更新 `.codex-api-spec/` 接口对接文档。

---

### 阶段4：任务完成与知识沉淀

#### 子模块 AGENTS.md 维护（重要）
**时机**：完成一个开发任务后

**操作**：
- 新建或修改子模块的 `AGENTS.md` 文件
- **补充内容**：
  - 项目结构说明
  - 存在的坑点与注意事项
  - 重要技术决策与理由
  - 技术决策、实现经验
  - 模块约定、已知问题


- 可选优化
  - 原有文件描述错误或不清晰的内容

**位置**：`子模块目录/AGENTS.md`

**层级**：`支持多层AGENTS.md，需覆盖到最小模块`

#### memory 记录
**记录时机**：
- 用户提出个性化要求或值得记录的信息时
- 对未来任务有复用价值时

**记录内容**：
- 用户偏好
- 用户画像
- 用户重点强调的信息

**记录原则**：
- **更新优先**：已有记忆则修改完善而非重复新建
- **删除过时**：对可明确判断为过时的记忆予以删除

#### 知识层级说明
- **memory**：用户个性化记忆（跨项目、长期有效）
- **子模块 AGENTS.md**：模块共享记忆（项目内、模块级）

---

## 4. 编码策略

- 优先复用官方 SDK/主流生态/既有组件
- 发现缺陷优先修复，再扩展功能
- 小步修改，每次变更保持可编译可验证
- **注释使用中文**：描述意图、约束、使用方式
- **方法命名应简洁高效**：名称聚焦核心语义，不在方法名中堆叠实现细节、条件分支或完整接口规则，参数约束、边界行为、错误语义等应在方法注释或接口文档中说明。
- 遵循 语言标准风格 + 项目既有风格
- 对"当前验收契约"给出完整可运行实现
- **不做过早抽象**：重复出现 ≥3 次再考虑通用化；除非验收契约明确要求扩展性
- 采用破坏性变更策略，不保留向后兼容（若验收契约未要求兼容）
- 只保留必要的安全性设计，避免过度设计
- 编码前必须确认：依赖、输入输出、测试约定
- **涉及中文字符使用 UTF-8 保存**（推荐 UTF-8 无 BOM），正确写入 Unicode；写入含反引号文本时使用单引号 here-string 写入，避免乱码

---

## 5. 测试分层与执行规则（单点定义，其他章节仅引用）

### 总原则
- 除必须由用户手动进行的测试之外，**全部测试/验证必须由本地 AI 自动执行**
- 允许本地脚本/本地命令；**禁止任何 CI/远程流水线/人工验证**
- 测试执行与结果写入 2.1 的单文档对应章节

### 分层定义

#### 1) 变更后冒烟测试（阶段2：每次改动后最低要求）
- **目标**：快速发现明显断裂（编译/启动/关键路径）
- **范围**：最小但可执行的验证命令或用例

#### 2) 子模块回归（阶段2：完成一个子任务/里程碑后）
- **目标**：确认该子模块行为未回归
- **范围**：与本次改动相关的单测/集成测试/样例运行

#### 3) 阶段3完整验证（阶段3：交付前必做）
- **目标**：覆盖正常流程 + 边界条件 + 错误恢复
- **范围**：项目既有测试编排中与需求相关的完整集合（在本地执行）

### 失败处理
- **失败必须记录**：现象、复现步骤、初步观察、修复尝试
- **同一问题连续失败 ≥3 次**：暂停并复盘策略（见阶段3）

---

## 6. 文档策略

- 按需要更新文档，自主规划结构
- 生成文档需标注日期与执行者身份
- 引用外部资料需标注来源 URL 或文件路径
- 日志/报告/验证输出等均遵循"路径单点规则"
- 仅后端项目在交付前必须更新 `.codex-api-spec/`，并确保路径使用 `/path/path` 形式
- 接口文档中所有示例需可用于前端直接联调（字段名、类型、错误语义一致）

---

## 7. 工具协作与降级

- **memory**：任务开始前按需使用（见2.2）；任务完成后或用户提出个性化需求时按需写入（见阶段4）
- **子模块 AGENTS.md**：修改模块前必读；任务完成后必须更新
- **工具不可用**：评估替代方案或报告用户，并记录原因与措施

---

## 8. 开发哲学

- **渐进式迭代**：每次改动可编译、可验证
- **研读既有实现**并复用经验
- **务实满足需求**，拒绝理想化过度设计
- **偏向简单清晰**：可读性优先，拒绝炫技
- 遵循项目既有代码风格、测试编排、构建运行方式

### 简单性定义
- 单一职责
- 尽量使用一个大方法+分块注释的方式而不是多个小方法
- 相同逻辑重复 ≥3 次再拆分，不做过早抽象
- 不使用"聪明技巧"，可读性优先
- 需要额外解释 ⇒ 继续简化

---

## 9. 行为准则

### 默认自动执行
- 读写文件、代码修改、文档更新
- 测试执行、依赖安装、构建编译
- 工具调用、子模块 AGENTS.md 更新

### 决策原则
- 若不在"例外清单"中 → 自动执行
- 如有疑问 → 自动执行并留痕
- 连续三次失败：自主复盘；当需要重要决策时再询问用户

### 极少数需要用户确认的情况（例外清单）
- 删除核心配置文件
- 数据库 schema 的破坏性变更
- Git commit 或 push 到远程仓库
- 策略调整将改变需求范围/验收契约/破坏性影响（含用户明确要求确认的操作）

---

## 协作原则总结

- **我规划，我决策**
- **我观察，我判断**
- **我执行，我验证**
- **我记录，我沉淀**
- **仅在触发例外项时询问用户**

---

## 附录：快速检查清单

### 任务开始前
- [ ] 读取 memory
- [ ] 读取子模块 AGENTS.md（修改模块前强制）
- [ ] 使用 sequential-thinking 深度分析（L2 任务强制）
- [ ] 使用 task-manager 规划任务（L2 任务强制）

### 任务执行中
- [ ] 每次改动后执行冒烟测试
- [ ] 关键决策与问题记录到文档
- [ ] 使用 code-index 优先搜索代码

### 任务完成后
- [ ] 执行完整验证（阶段3）
- [ ] 全栈/前端项目：提醒用户完成前后端部署后，使用chrome-devtools检查 UI 布局并优化
- [ ] 仅后端项目：提醒用户重新部署后，自动编写测试用例并用 `curl` 或 Python 调用接口验证返回值
- [ ] 仅后端项目：按 2.3 更新 `.codex-api-spec/` 接口对接文档
- [ ] 更新或新建子模块 AGENTS.md
- [ ] 按需记录到 memory
- [ ] 生成完整留痕文档

---

## 项目结构补充（searxngagent）

### 当前结构
- `src/news_query_framework/`：通用全球新闻 Query Pack 框架实现
- `examples/`：默认 Query Pack、引擎映射和任务请求样例
- `sql/`：MySQL 初始化建表脚本
- `tests/`：本地 `unittest` 验证
- `search_api.rst`：SearXNG Search API 参数约束参考

### 重要约定
- 默认 SearXNG 接入固定为 `http://10.232.1.101:3080/search`，CLI 可覆盖 base_url 但默认值不能改丢。
- 逻辑引擎组与实例真实引擎名必须分离：`Query Pack.engine_groups` 只表达逻辑组，真正发请求前必须经过 `engine_map.json` 校验与映射。
- 主题与事件关键词优先外置在配置中，不要把新增行业写死进规划器逻辑。
- `keywords.local` 已支持按市场定义：允许 `local: {JP: {...}}`，也允许多本地语市场使用 `local: {EU: {de: {...}, fr: {...}}}`；规划器会优先取市场本地桶，再回退到语言桶与 `en`。
- 本地语词表优先覆盖政治与监管类槽位；查询规划器缺词时允许回退到 `en`，但不要静默丢掉该槽位。
- 补召回必须覆盖“主召回完全为空”的槽位，不能只处理“结果少于阈值但非空”的情况。
- 政治类补召回除结果量/引擎数/权威源阈值外，还要识别“只有转载、没有原发或官方源”的场景。
- 本地运行与测试默认使用 `PYTHONPATH=src`，否则 `unittest` 无法直接导入包。
- MySQL 入库以 `news_article.url_hash` 为主去重键，必须走 UPSERT，禁止重复插入主表。
- 禁止保存原始 SearXNG 整包 JSON，只允许保存清洗后的 `news_article` 和 `news_article_hit`。

### 已知边界
- 当前跨语言聚合主要基于 URL 规范化和标题指纹，若后续要做更强的跨语种事件归并，应新增语义级相似度层而不是把复杂逻辑塞回规划器。
- 默认执行是串行模式，后续若做并发，应保证 coverage_report 与 hit_queries 聚合结果保持可重复。
- 来源层级与原发性判断当前主要依赖域名规则和 trusted domains，适合第一版高覆盖框架；若后续要精细识别转载链路，应新增专门的来源画像层。
- 当前仓库通过 `PyMySQL` 连接 MySQL；若运行环境缺少驱动，需要先安装 `PyMySQL`。



注意,你被禁止真实删除任何文件,要删除的话,将删除指令修改为剪切到当前文件夹的delete文件夹下(没有就新建一个)
