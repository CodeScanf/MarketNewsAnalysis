# IntAnalysis 启动文档

## 1. 环境准备
- Python 3.9+
- Node.js 18+
- 建议使用虚拟环境（`venv` 或 conda）

安装后端依赖：

```bash
pip install -e .
pip install baostock
python -m spacy download en_core_web_sm
```

## 2. 启动后端

在项目根目录执行：

```bash
uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
```

启动后可访问：
- Swagger: `http://localhost:8000/docs`
- 健康检查: `http://localhost:8000/health`

## 3. 启动前端

新开终端执行：

```bash
cd frontend
npm install
npm run dev
```

前端地址默认：`http://localhost:5173`

## 4. 首次使用流程
1. 打开前端页面，先注册账号并登录（Cookie 会话鉴权）。
2. 进入 Chat / Knowledge Base / Daily Recommendations / Stocks 视图使用功能。
3. 股票查询接口（需登录）：
   - `POST /stocks/basic`
   - `POST /stocks/kdata`
   - `POST /stocks/index`
   - `POST /stocks/valuation`

## 5. 常用测试命令

```bash
pytest -q tests/test_stocks_service.py tests/test_stocks_api.py tests/test_auth_api.py
```

如需前端构建验证：

```bash
npm --prefix frontend run build
```

## 6. 常见问题
- `401 Authentication required`：未登录或会话过期，重新登录即可。
- `502`（股票接口）：BaoStock 上游异常或依赖不可用，请检查 `baostock` 安装与网络。
- 前端无数据：先确认后端在 `8000` 端口正常运行。
